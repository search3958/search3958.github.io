(function(){"use strict";const CheckJS ={CONFIG:{AD_SCRIPT_URL:'https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-6151036058675874',ERROR_URL:'https://search3958.github.io/aderror.html',ENTRY_URL:'https://search3958.github.io/entry.html',FETCH_TIMEOUT:8000},async init(){console.log("✅🟢 CheckJS-正常に読み込まれました");if(localStorage.getItem('termsAccepted')=== 'false'){window.location.replace(this.CONFIG.ENTRY_URL);return;}try{console.log("✅🟡 CheckJS-確認中(1回目)");const controller = new AbortController();const timeoutId = setTimeout(()=> controller.abort(),this.CONFIG.FETCH_TIMEOUT);const response = await fetch(this.CONFIG.AD_SCRIPT_URL,{signal:controller.signal});clearTimeout(timeoutId);if(!response.ok)throw new Error('Network error');const text = await response.text();const isLengthValid = text.length >= 5000 && text.length < 200000;const lineCount =(text.match(/\n/g)|| []).length;const isLineCountValid = lineCount < 1000;const hasLicense = text.includes('SPDX-License-Identifier')&& text.includes('Apache-2.0');if(!isLengthValid || !isLineCountValid || !hasLicense){this.handleFailure("Validation Details:",{length:text.length,lines:lineCount,license:hasLicense});return;}console.log("✅🔵 CheckJS-検証済み(正常)");const script = document.createElement('script');script.textContent = text;document.head.appendChild(script);this.verifyObjectPresence();}catch(error){this.handleFailure("問題があります");}},verifyObjectPresence(){setTimeout(()=>{console.log("✅🟡 CheckJS-確認中(2回目)");if(!window.adsbygoogle){this.handleFailure("問題があります");}else{console.log("✅🔵 CheckJS-検証済み(最終正常)");}},2500);},handleFailure(reason,details = null){console.error(`✅🛑 CheckJS-${reason}`);if(details)console.error(details);window.location.replace(this.CONFIG.ERROR_URL);}};if(document.readyState === 'loading'){document.addEventListener('readystatechange',()=>{if(document.readyState === 'interactive')CheckJS.init();});}else{CheckJS.init();}})();